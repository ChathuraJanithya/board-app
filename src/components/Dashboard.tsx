"use client";

import { ProjectHeader } from "@/components/ProjectHeader";

export function Dashboard() {
  return (
    <div className="h-full flex w-full flex-col bg-gray-50">
      <ProjectHeader />
    </div>
  );
}
